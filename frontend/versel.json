{
    "version": 2,
    "builds": [
        {
            "src": "pakage.json",
            "use": "@versel/static-build",
            "config": {"distDir": "dist"}
        }
    ],
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}